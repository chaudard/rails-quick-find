<%= simple_form_for "", url: searches_path do |f| %>
  <%= f.input :keywords %>
  <div id="autocomplete">
  <%= f.input :input_address %>
  </div>
  <%= f.input :distance, collection: Search.distances %>
  <%= submit_tag(value = "SUBMIT", class: "btn-medium") %>
  <% end %>
